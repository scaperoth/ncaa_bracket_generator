<nav id="menu">
	<h2>Menu</h2>
	<ul class="navbar">
		<% if defined?(Devise) %>
		<% if user_signed_in? %>
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">Account
				<b class="caret"></b>
			</a>
			<ul class="dropdown-menu">
				<li>
					<%= link_to "Edit Account", edit_user_registration_path %>
				</li>
				<li class="divider"></li>
				<li>
					<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
				</li>
			</ul>
		</li>
	<% else %>
		<li>
			<%= link_to "Login", new_user_session_path %>
		</li>
		<li>
			<%= link_to "Sign Up", new_user_registration_path %>
		</li>
		<% end %>
		<% end %>

		<li class="nav-item dropdown">
			<a href="<%= archive_path %>" class="nav-link dropdown-toggle" data-toggle="dropdown">
				Archive
				<b class="caret"></b>
			</a>
			<ul class="dropdown-menu">
				<% available_brackets.each do |tournament| %>
				<li class="dropdown-item">
					<%= link_to tournament.name, bracket_comparison_path(year: tournament.year) %>
				</li>
				<% end %>
			</ul>
		</li>
	</ul>
</nav>